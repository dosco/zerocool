# Codebase audit, September 2026

A review of the whole repository: the native engine, the HTTP API and terminal
client, the Python tooling, the build system, and how the repository itself is
organized. Every finding below was checked against the source; the verification
column says how. Findings marked **fixed** landed in commits `780bd5d` through
`7329a6c`.

## Method

The engine (`src/engine`, `include/engine`, `kernels/metal/qwen.metal`, `tests`) was
read in full. The Python tooling was sampled and measured with counting greps.
Claims about behaviour were confirmed by running the code: a clean-checkout
build, the native and transport test binaries, the 556-check Python suite, ruff
over `scripts`, and a purpose-built adversarial harness linked against
`libzerocool_lib.a` for the tokenizer finding. Repository measurements come from
`git ls-files` and `stat`, not estimates.

## Summary

| Area | Fixed | Open |
|---|---|---|
| Security | 2 | 0 |
| Correctness and latency | 6 | 3 |
| Build, tests and CI | 6 | 1 |
| Repository and documentation | 6 | 0 |
| Duplication and dead code | 1 | 5 |

## Security

### Message text could become chat control tokens — fixed

`Tokenizer::encode` recognizes every entry in the checkpoint's `added_tokens`
anywhere in its input, and the chat path handed it the entire rendered template
(`encode(render(...))`). The pinned checkpoint's added tokens include
`<|im_start|>`, `<|im_end|>` and `<|endoftext|>`, so a user or tool message whose
text contained an end-of-turn marker followed by a system header was encoded as
genuine turn boundaries. A coding client that pastes file contents into a
message could therefore let a file on disk forge a system or assistant turn.
Neither request validation nor the template escaped or rejected those strings.

`Tokenizer::encode_chat` now substitutes a private-use marker for control-token
text in message content, reasoning, tool calls and tool declarations, renders,
then encodes those bytes as ordinary text. Only the template itself emits turn
boundaries.

*Verified:* against the real tokenizer with 26 assertions in
`tests/test_qwen.cpp`. Ordinary prompts tokenize byte-identically to the former
path, so no retained prefix or recorded measurement changes. Five injected
payloads keep their exact text while adding no turn boundaries. The chat
template was inspected to confirm it renders only `content`,
`reasoning_content`, `tool_calls`, `role` and `tools`; the first three and
`tools` are guarded, `role` is validated against four fixed strings, and
`reasoning_effort` is validated by the template against three values and never
echoed into the prompt.

### The loopback listener was reachable from a browser — fixed

Request parsing checked neither `Host`, `Origin` nor `Content-Type`. Because a
POST body was accepted regardless of content type, any web page could send a
cross-origin "simple" request to `http://127.0.0.1:PORT` and occupy the single
inference slot or reset the retained session, and a rebound DNS name could drive
the engine and read `/zerocool/status`. The optional instance header was no
defence, since it is only checked when present.

The server now refuses a request carrying an `Origin` header with 403, a
non-loopback `Host` with 403, and a POST without
`Content-Type: application/json` with 415, which forces a CORS preflight that
fails because no CORS headers are ever returned.

*Verified:* raw-socket tests in `tests/test_chat_transport.cpp` covering each
rejection and the accepted loopback forms, including a case-insensitive header
name and a `Host` with a port.

## Correctness and latency

### Retained state was discarded on every non-200 and on cancellation — fixed

The inference loop cleared the session for any non-200 outcome, including
rejections that never touched it, such as the context-overflow check, parameter
validation, and a malformed tool call in an otherwise complete response.
`Session::generate` also cleared on any exception, and the cancellation check at
the top of the decode loop fires *after* the previous forward committed, so
cancelling between decode steps threw away a consistent prefix. Each case cost a
re-ingest of up to 8192 tokens on the next turn.

`Session::reusable()` now reports whether the state is still continuable, and
`ChatExecutor::recover()` clears only when it is not. This is safe because
`Model::make_state` sets `valid`, `StateUpdate`'s constructor clears it, and only
`commit()` restores it, so a failed forward is always unrollable.

*Verified:* by reading the state lifecycle in `src/engine/model.cpp` and confirming
the reuse condition now also requires `state_->valid`.

### The first chat request paid kernel pipeline creation — fixed

`NativeChat::initialize` never called `Model::prepare_pipelines()`; only the
benchmark and cached-replay paths did. Metal pipeline states are created lazily
on first dispatch, so the first served request paid that cost on top of the
per-process Metal source compile. Initialization now builds them before
reporting ready, under a `preparing_kernels` phase.

### Request parameters were coerced rather than checked — fixed

Parameters were read with nlohmann's `value()`, which calls `get<T>()` on
whatever type is present. A float `max_tokens` was silently truncated, a null
produced a raw library error string as the user-visible message, and an
out-of-range double was converted with an out-of-range `static_cast<int>`, which
is undefined behaviour. The same applied to `stream` and
`stream_options.include_usage`. All are now checked by type, with messages
naming the parameter, and text-part content is accepted since the template
already supported it.

*Verified:* 20 assertions in `tests/test_chat_transport.cpp`, including the
float, the out-of-range double, the wrong-typed string and the explicit null.

### `main()` was 400 lines with untestable validation — fixed

Command-line parsing and about fifteen cross-option rules lived inside `main()`,
and several rules were duplicated against the checks in `Model`'s constructor.
`parse_cli` and `validate_cli` now live in the library.

*Verified:* 23 assertions in `tests/test_qwen.cpp` covering accepted forms and
rejected combinations, which were previously unreachable from any test.

### Magic numbers — partly fixed

The two stop-token ids appeared as literals in twelve files and the 8192 context
bound in eight. `MaxContext`, `EndOfText`, `ImEnd` and `is_stop_token` are now
named in `include/engine/storage.hpp` and used by the engine. Other repeated
literals remain, listed under open findings.

### Stale reference in the engine document — fixed

`docs/qwen_engine.md` still told readers the previous multi-backend
implementation was available behind `ZEROCOOL_BUILD_LEGACY`, which no longer
exists.

## Build, tests and CI

| Finding | Evidence | Resolution |
|---|---|---|
| No default build type | Neither `CMakeLists.txt` nor `cmake/qwen.cmake` set `CMAKE_BUILD_TYPE`; only `build.sh` passed it, so a bare `cmake -S . -B dir` produced an unoptimized engine | Defaults to Release with a message |
| Build identity ignored how it was built | The fingerprint hashed sources and locks only, so Debug and Release reported the same `build` value in every benchmark JSON and in `/zerocool/status` | Compiler, version, build type, flags and sanitizer are hashed in |
| Warnings covered 2 of 17 targets | `-Werror` applied to `zerocool_lib` and the chat client; the engine executable, all tests and the 13 diagnostics compiled without it | One `zerocool_warnings` interface target, linked everywhere |
| No sanitizer or preset | No `-fsanitize` anywhere, no `CMakePresets.json` | `ZEROCOOL_SANITIZE` plus release, debug, asan, tsan and no-tui presets |
| Mutable dependency pin | nlohmann/json was pinned to tag `v3.11.3`; libcurl was linked by bare name | Pinned to commit `9cca280`; `find_package(CURL)` |
| `ctest` ran 2 tests | The 556 Python checks were only reachable from a command inside a document | Registered as `PythonTooling`, preferring the repository `.venv` |
| No CI, formatter, lint or editor config | None of `.github`, `.clang-format`, `.editorconfig`, `pyproject.toml`, `.pre-commit-config.yaml` existed | macOS workflow building and running the whole suite, plus a ruff job; clang-format, editorconfig and ruff configuration |

Enabling ruff surfaced two real defects, now fixed: `scripts/qwen/trial_recovery.py`
used the `fixtures` and `correctness` stage directories before assigning them,
relying on cross-iteration assignment that held only because of the stage order;
and 26 unused imports plus three dead assignments had accumulated.

## Repository and documentation

The repository tracked **11,208 files and about 677MB**, of which
`docs/benchmarks` was **10,643 files and 665MB**. It contained Metal command
dumps up to 35MB each, expert weight fixtures, raw logit dumps, two SQLite
databases, vmmap and heap dumps, and **5,785 copied source files** under
per-stage `*-sources/` directories holding only **374 distinct contents**. One
directory archived an entire build tree including a static library. No snapshot
matched any committed version of the file it copied, so the evidence tree was
standing in for commits that were never made: 17 commits since 9 September
against 70 dated benchmark directories.

The 26 per-stage un-ignore rules had already drifted. About 200 tracked files
matched a current ignore rule, while about 558 files inside tracked stage
directories were silently excluded, so what reached git varied stage to stage.

The committed set is now the stage reports, their small summary, analysis and
decision JSON, the 392KB experiment ledger that `query_evidence.py` reads, and
the ten evidence files scripts consume as fixed inputs: **926 files, 21.4MB**.
Nothing was deleted from disk and nothing was removed from history.

`AGENT.md` was 87% stale. Its current-direction section was followed by guidance
describing a CPU-only engine, a TinyLLaMA target, x86 AVX2 flags, GCC 15, "no
JSON library", a test suite in a file that no longer exists, a header tree that
had moved, and a download script that was never in the repository. `README.md`
was a chronological changelog of benchmark screens: a reader passed eleven
measurements before reaching the build instructions, and only six of the 31
documents were linked from anywhere. Five documents were referenced by nothing
at all. Both were rewritten and `docs/README.md` now indexes everything.

### The legacy tree was dead

The educational CPU/CUDA/TinyLLaMA implementation was reachable only through
`ZEROCOOL_BUILD_LEGACY`, shared no code with the engine, and had not been touched
since the commit that introduced the engine. A grep for references from the
engine, its tests, the Metal kernels and the build files into
`include/core`, `include/infra`, `include/kernels`, `src/core`, `src/infra` or
`src/examples` returned nothing. It was removed with its ten test binaries, its
design documents, and the stale root files that served only it: a tracked Mach-O
benchmark binary whose source included headers that had already moved, and two
shell scripts building legacy-only targets in the wrong build directory. That is
116 files and 33,624 lines.

## Open findings

These were verified but not fixed, because each is a separate change with its own
risk.

| Finding | Evidence | Suggested fix |
|---|---|---|
| No model-free test seam | `Session` takes a concrete `Model&`, so `Model`, `Session`, `Tokenizer` and `NativeChat` have no model-free tests; the tokenizer test added here is skipped when the checkpoint is absent | A small forward-engine interface, or template `Session` on the engine type, plus a scripted fake |
| Duplicated expert schedule | The non-completion-pipeline branch of `Model::moe` and `execute_experts_batched` in `src/engine/pipeline.cpp` implement the same 32-expert window; both derive it as `std::min<size_t>(32, capacity())` | Call `execute_experts_batched` from `moe()` |
| Dead Metal kernel | `kernel void embedding` at `kernels/metal/qwen.metal:380` is never dispatched; only `affine_embedding` is. The `"embedding"` strings in `model.cpp` and `prefill.cpp` are phase labels | Delete it; it also shortens the runtime compile |
| SHA-256 hex loop copied | 24 C++ files contain the hex digit table | One `sha256_hex` helper in `storage.hpp` |
| Python hashing duplicated | 76 of 244 scripts define their own `hashlib.sha256` helper although 123 already import the shared `qualification_evidence` module | Move the helper into the shared module |
| Latent double close | `struct Socket` in `src/engine/server.cpp:22` is copyable with a closing destructor; `RouteTrace` and `MemoryTrace` delete the copy constructor but leave copy assignment implicitly defined over a `FILE*` | Delete both operations, or hold the handle in a `unique_ptr` |
| Brittle source patching | Nine Python scripts build instrumented engine binaries by string-replacing into `src/engine/*.cpp`; a formatting change to the matched text breaks them | Guard each patch with a test, as `test_build_perfect_draft.py` already does for one |
| Four orphan scripts | `build_mtp_check`, `check_mixed_reference`, `verify_q4_packed` and `verify_resident_operators` are referenced by no document, test or other script | Confirm the experiments concluded, then remove |
| Tree not formatted | `.clang-format` matches the house style but `clang-format` is not installed on this machine, so the tree was not reformatted and no format gate was added to CI | Install it, reformat as its own commit, then add the gate |

### Reported but not independently verified

These came from an automated review pass and were not reproduced here. They are
recorded so they are not lost, not asserted as true.

- Parsing untrusted bodies with `ordered_json`, whose object lookup is a linear
  scan, may be quadratic for a large object within the 4MiB body limit.
- Per-dispatch overhead from building an `NSString` and doing `ordered_json`
  lookups on every dispatch.
- A thread spawned per forward by `std::async` for the ngram read fan-out.
- `SO_SNDTIMEO` at two seconds may cancel a streaming client that pauses reading,
  which with the session change above no longer also clears the session.

## Verification performed

| Check | Result |
|---|---|
| Clean-checkout build from committed `HEAD` | exit 0 |
| Native tests on that checkout | 74 cases, 49,224 assertions pass |
| Transport tests on that checkout | 10 cases, 1,224 assertions pass |
| `ctest` on the working tree | 3 of 3 suites pass |
| Python suite | 556 tests pass in about 21s |
| `ruff check scripts` | clean |
| Files tracked before the change now unexpectedly missing | 0 of 11,271 |
| Tracked files the ignore rules would exclude | 0 |

## Caveat: checking out across the evidence change

The commit that untracks the raw evidence removes those files from the working
tree on any checkout across it, because git deletes files that a commit stops
tracking. That is inherent to untracking, not a defect. The data remains in
history at `f26f813` and on disk. Archive `docs/benchmarks` before sharing this
branch. To restore after a checkout:

```sh
git restore --source=f26f813 --worktree -- docs/benchmarks docs/experiments
git restore --worktree -- docs/benchmarks docs/experiments
```

This was exercised during the audit and restored all 10,760 files with none
missing.
