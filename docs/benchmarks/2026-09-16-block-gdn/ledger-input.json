{
  "hypothesis": "Pairing two output rows in the existing four-token Q8 GDN kernels can save at least 10ms per input token in a shape-frequency screen and justify full-verifier testing.",
  "expected_effect": "Exact outputs, clean fresh operator processes, five alternating pairs with summed GPU-ratio upper95 below1 and median isolated shape-frequency projection at least10ms/token.",
  "controlled_change": "Only existing Q8 output-row count1 to2, with token tile4 and unchanged arithmetic, on three real layer0 GDN fixtures at decode offset72. Capture and production remain unchanged in arithmetic.",
  "correctness": "Complete clean profile covers192 dependency passes and21747 dispatches per mode. Fixture capture matches full logits/routes/state/prime cache; all9 Q8 weight/scales/biases match pinned source. Fresh Metal validation and all paired operator outputs are bitwise equal to scalar reference.11 focused tests, native configuration self-test and independent audits pass.",
  "outcome": "Independent replay completes in4.36seconds with clean memory and host. Summed isolated GPU ratio0.9026803, paired95 interval0.8909310-0.9145845. Median projection2.9394ms/token misses10ms gate. No full-verifier timing or production promotion. Source capture remains memory-blocked; only exact tensor bytes are reused, never its timing.",
  "decision": "rejected",
  "smallest_experiment": "Test packed32-bit Q8 weight loads for four-token execution, retaining one output row and scalar/SIMD order, on the verified fixtures. Require material isolated benefit before full-verifier tests; do not repeat row-pair or cache-policy screens.",
  "limitations": [
    "The candidate has a small real isolated speedup; rejection applies to advancement at the declared materiality gate.",
    "Layer0 inputs and repeated weights do not cover full inference, other layers, long contexts or drafting.",
    "Shape-frequency projections are not measured request savings. Prior4.07serial/4.67perfect-verifier timings remain unchanged.",
    "The original combined stage remains resource-blocked. Fresh independent operator processes use only verified payload bytes."
  ],
  "evidence": [
    "/repo/docs/benchmarks/2026-09-16-block-compute/capture-03/summary.json",
    "/repo/docs/benchmarks/2026-09-16-block-gdn/replay-01/summary.json",
    "/repo/docs/benchmarks/2026-09-16-block-gdn/screen-03/summary.json"
  ]
}
