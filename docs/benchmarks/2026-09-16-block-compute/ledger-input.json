{
  "hypothesis": "Complete four-token GPU and expert-read profiles can identify one compute experiment that matters to verifier latency at CLOCK 1460 and 12GiB.",
  "expected_effect": "Diagnostic evidence only: full block coverage, exact model results, observed overlap and actual per-expert row geometry. No throughput prediction.",
  "controlled_change": "Developer source copies enable existing command and dispatch profiling only for decode. Same 72-token prime and 16 continuation inputs, all startup blocks retained, 512MiB profiling allowance within unchanged admission.",
  "correctness": "Both native developer binaries build; seven focused offline tests and source/evidence audit pass. Native profiling and full-model profile parity have not run because admission stopped before loading.",
  "outcome": "Initial attempt remains sealed as failed and incomplete; exact error is insufficient admitted expert capacity with 10.449GiB reclaimable. Host-only follow-up reports 9.975GiB, below 13.5GiB needed for fixed 12GiB plus native safety margin. Future identical pre-forward capacity errors are classified resource_blocked. Prior route replay independently gives 3110 one-row, 1143 two-row, 452 three-row and 232 four-row expert calls; no new timing.",
  "decision": "blocked",
  "smallest_experiment": "After material host headroom improvement, run the unchanged two-mode profile in a fresh output directory and audit all four blocks. Select one actual-input replay from clean evidence.",
  "limitations": [
    "No GPU profile, kernel ranking, latency improvement or production promotion.",
    "The engine has not loaded weights in this attempt. Current host headroom is separate from prior measured engine footprint.",
    "Offline expert row counts do not attribute runtime cost; current native selector already clamps one-row tiles."
  ],
  "evidence": [
    "/repo/docs/benchmarks/2026-09-16-block-compute/capture-01/summary.json",
    "/repo/docs/benchmarks/2026-09-16-block-compute/route-geometry.json",
    "/repo/docs/benchmarks/2026-09-16-block-compute/headroom-02.json"
  ]
}
