# Separate numerical diagnosis from clean timing

Registered after the preserved validation-02 resident run recorded 31.21875MiB
process compression, and before any candidate full-model run or timing sample.
The numerical inputs and producer remain unchanged. The ten real embedding
fixtures passed cleanly; the full resident process did not pass the resource gate.

Reuse that sealed resident width-one result only for numerical comparison after
rechecking its input, native producer and complete raw output. Run the remaining
six-pair matrix with Metal validation. A numerical diagnostic may contain at most
128MiB peak compression, must stay within the original 12GiB physical/process
bound, and must retain unchanged swap, nominal thermal state and AC power. This
is the existing horizon diagnostic rule. Record every original cleanliness flag;
never relabel a compressed run or its timings as qualified.

Once every numerical pair matches, an explicitly preliminary storage-cost screen
may run the original fresh normal pairs. Every timing process still requires
zero compression/decompression, unchanged swap and all original resource guards.
The preliminary result can reject a costly storage path or identify further work;
it cannot authorize adoption, bypass clean correctness, or satisfy promotion.
Keep full_clean_correctness false while any required numerical process is dirty.
No old or diagnostic timings enter a ratio or generated-token speed claim.
